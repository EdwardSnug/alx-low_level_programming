#include "main.h"
/**
  *_strpbrk - A function that locates the first occurence of given string
  *
  *@s: The character to cross check for occurence
  *@accept: Number of bytes to cross check for character
  *
  *Return: Returns byte found or NULL if none is found
  */
char *_strpbrk(char *s, char *accept)
{

	return (s);
}
