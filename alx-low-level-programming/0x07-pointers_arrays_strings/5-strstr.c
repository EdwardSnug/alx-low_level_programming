#include "main.h"
/**
  *_strstr - Function that finds first occurence of 'needle' in the stored
  * string variable 'haystack' withount null byte compaison.
  *
  *@haystack: Stored string variable
  *@needle: Substring to be located in stored variable
  *
  *Return: Located substring(needle) or NULL if not found
  */
char *_strstr(char *haystack, char *needle)
{
	return (0);
}
