#include "main.h"
/**
  *positive_or_negative - A function that input numbers under conditions
  *
  *@i: Integer value any number
  *
  *Return: Returns integer value i
 */
void positive_or_negative(int i)
{
	if (i > 0)
	{
		printf("%d is positive\n", i);
	}
	else if (1 < 0)
	{
		printf("%d is negative\n", i);
	}
	else
	{
		printf("%d is zero\n", i);
	}
}
